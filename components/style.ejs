<%#
    Style Partial
    Theme: Hubble JS Default
    Render Theme Styles
    @param { Object } theme data
%>
<style>
    .is-primary {
        color: <%= theme.palette.primary %>;
    }
    .is-secondary {
        color: <%= theme.palette.secondary %>;
    }
    .header {
        content: '<%= theme.headerPattern %>';
    }
</style>