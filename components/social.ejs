<%#
    Social Partial
    Theme: HubbleJS Default
    Render Social Media Links
    @param { Object } social
%>
<div class="social-media">
    <ul>
        <% Object.keys(social).forEach(function (key) { %>
            <li class="social-media__link <%= key %>">
                <a
                    href="<%= social[key] %>"
                    title="<%= user.name %> on <%= key %>"
                    rel="noopener noreferrer"
                    target="_blank"
                >
                    <%- include(`../src/icons/${key}.svg`); %>
                </a>
            </li>
        <% }); %>
    </ul>
</div>