<%#
    Styles
    Theme: Hubble JS Default
    @param { Object } theme, utilities
%>
<style>
    <%# Theme Options %>
    :root {
        --primary: <%- theme.palette.primary %>;
        --secondary: <%- theme.palette.secondary %>;
        --text-primary: <%- theme.palette.textPrimary %>;
        --bg: <%- theme.palette.background %>;
        --bg-rgb: <%- theme.palette.backgroundRGB %>;
        --bg-accent: <%- theme.palette.backgroundAccent %>;
        --bg-accent-light: <%- theme.palette.backgroundAccentLight %>;
    }

    <%# Theme Pattern %>
    .pattern-bg {
        background-size: <%- theme.pattern.size %>;
    }

    <%# Theme Social %>
    <% if (theme.social !== undefined) { %>
        <% Object.keys(theme.social).forEach(function (key) { %>
            .<%= key %> {
                background-color: <%= utilities.getSocial(key).primary %>;
            }
            .<%= key %>:hover {
                background-color: <%= utilities.getSocial(key).hover %>;
            }
        <% }); %>
    <% } %>
</style>

<link rel="stylesheet" href="assets/project.min.css">
<link rel="preload" as="style" href="assets/deferred.min.css"/>